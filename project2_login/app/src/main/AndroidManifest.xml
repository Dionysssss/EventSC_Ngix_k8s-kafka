<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MapRendererOptInApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project2_login"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCN6PowW-UE7LwpHIOTp6KRojRApbabW_U" />

        <!-- LoginView should now be the launcher activity -->
        <activity
            android:name=".LoginView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterView activity declaration -->
        <activity android:name=".MapViewActivity" android:exported="true" />

        <!-- CreateEventActivity for entering event details -->
        <activity android:name=".CreateEventActivity" />

        <activity android:name=".EventDetailsActivity" />

        <!-- ModifyEventActivity declaration -->
        <activity android:name=".ModifyEventActivity" android:exported="true"/>



    </application>

</manifest>
